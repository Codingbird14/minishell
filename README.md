# minishell
  A mini desktop shell

# Usage
  - Copy `minishell` to your quickshell config
  - Add GPS location and your openweatherapi key (currently still on version 2.5)
  - Run `qs -c minishell` to run

# Notice
  This small project is still in the development phase, so expect nothing to work
